package com.ratedpeople.service.utility;

public class HttpGIDClientConnection {

	/*
	 * private static final String FILE_KEYSTORE = "keystore.jks"; private static final String PASSWORD = "ratedpeople";
	 * 
	 * private RestTemplate restTemplate;
	 * 
	 * public String callGetRest(final String endpointUrl) throws Exception { LOGGER.debug("call get endpoint " + endpointUrl); ResponseEntity<String>
	 * postcodeEntity = getRestTemplate().getForEntity(endpointUrl, String.class); return postcodeEntity.getBody(); }
	 * 
	 * private RestTemplate getRestTemplate() throws Exception { if (restTemplate == null) {
	 * 
	 * KeyStore keyStore = KeyStore.getInstance(KeyStore.getDefaultType()); keyStore.load(new FileInputStream(new File(FILE_KEYSTORE)), PASSWORD.toCharArray());
	 * SSLConnectionSocketFactory socketFactory = new SSLConnectionSocketFactory(new SSLContextBuilder() .loadTrustMaterial(null, new
	 * TrustSelfSignedStrategy()).loadKeyMaterial(keyStore, PASSWORD.toCharArray()).build()); HttpClient httpClient =
	 * HttpClients.custom().setSSLSocketFactory(socketFactory).build(); ClientHttpRequestFactory requestFactory = new
	 * HttpComponentsClientHttpRequestFactory(httpClient);
	 * 
	 * restTemplate = new RestTemplate(requestFactory); } return restTemplate; }
	 */

}